
apiVersion: v1
kind: Secret
metadata:
  name: edx-platform-gitops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: https://github.com/BlackOpsSolutions/Training-ArgoCD.git
  project: default
  username: ${PAT_USERNAME}
  password: ${PAT_SECRET}
